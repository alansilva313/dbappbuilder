import { prisma } from "../../config/client/PrismaClient"


export default class PostesVerificados {

    async qtde(req: any, res: any){

        try {
            

            
            
        } catch (error) {
            console.log(error)
        }
    }

}